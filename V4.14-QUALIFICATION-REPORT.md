# v4.14 RC2 qualification evidence contract

This source document defines the scope of the RC2 candidate. The separately sealed FINAL-RC2-REPORT.md and RC2-QUALIFICATION-RECEIPT.json contain the actual final execution results, exact commit, hashes and release classification. They must accompany any release approval request. No result is inferred from this document or carried forward as a fresh run.

RC1 is preserved at d65a879b872f5f607ca87165e88f067d8f1e5ae5. Its source archive, report, evidence bundle and videos remain unchanged. RC2 uses a separate branch, disposable installations and separate evidence.

The numbered release corpus is frozen before modification: 38 cases, 75 applicable write/replacement operations, SHA-256 9e771da4d6939bf558cea066063384d16f355524788061d9312b148b0cd5c474. It includes reconstructed MUT-004/MUT-008 source copies, partial/mixed copies, source/read provenance, prose, tables, CSV, configuration, arrays, numeric code, comments and generated documentation. Reconstructed inputs are not described as recovered historical traces. The original RC1 synthetic corpus is retained separately, including examples whose claimed origin cannot be established from their bytes alone. Differences must be disclosed.

Installed-service testing uses the ordinary entry point, bootstrap, first-run discovery, Job bootstrap, RuntimeSafetySupervisor, admission, worker placement, artifact store, verification and owned cleanup. The existing qualification adapter is enabled only in the disposable environment. Controlled HTTP responses are explicitly NO MODEL INFERENCE. Legacy repair selection is explicit; native semantic calls remain strict. Startup/restart/reboot, failures and configuration/ledger persistence are separate recorded checks.

The five-task Qwen workload retains frozen task/model hashes and independent verification. Scope is five internal tasks, not external benchmark generalisation. The final receipt records actual successes, parser/translation failures, calls, dispatches, usage and durations; failures are retained. Local inference cost is NOT APPLICABLE and energy is NOT MEASURED unless independently measured.

Further gates are: 150-case repair corpus with zero false repairs; 41-case no-progress corpus plus physical cases; three-Job concurrency; pristine CPU-only installation; new-format downgrade and round trip; actual-archive privacy and forensic-exclusion tests; reproducible source builds and fresh-extraction checks; exact-final-commit regression; bounded security review; and evidence-bundle verification. Final live video is conditional on the software gates passing, is captured from actual runtime events, and must pass complete decode, final-frame and SHA-256 verification.

Only the final report may classify this candidate READY_FOR_RELEASE, EXPERIMENTAL or BLOCKED. Approval to merge, tag, publish or upgrade is a separate step.
