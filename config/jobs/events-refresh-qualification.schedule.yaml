apiVersion: agent-control/v1
kind: Schedule
metadata:
  id: events-refresh-twice-daily-candidate
  name: Events refresh twice-daily candidate
spec:
  enabled: false
  job: events-refresh-qualification@1.0.0
  cron: "0 7,19 * * *"
  timezone: Europe/London
  missedRunPolicy: run-once-immediately
  parameters:
    publish: false
