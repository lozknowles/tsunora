# Tsunora fork report

## Review gate

Proposed public repository: **lozknowles/tsunora**. Prepared locally only. Do not publish until repository creation and migration have been reviewed. Agent Control remains independent; its remote, public main/tags and running installations were not changed.

## Source and fork

Authoritative qualified release: Agent Control v4.15.0, 46442b62099b930b9d694ba4fde1109262f3047f. The specified 0071262a5 had a legitimate successor, c972256b77b94ec55d2466e961e673b09f5b9e6c, with AI allocation evidence and architectural disposition. Its experimental status is preserved. Both lineages are retained by a real merge in the new independent repository; no squashed or fabricated past.

Initial integration commit: 5ab3208358a3eba5e0f67bb51106582b9e0538eb. Final reviewed product commit is recorded separately in FORK-STATE.json to avoid a self-referential commit hash. Product branch research/tsunora-0.1. Inherited runtime dependencies/package lock, CLI/API/schema/persistence identifiers and release tags remain intact. There was no inherited GitHub Actions workflow in this baseline; no new public CI or release automation is implied.

## Product and architecture

Tsunora 0.1.0-dev is separate from Agent Control Runtime 4.15.0. The product entry /tsunora.html separates a sparse brand hero from Work, Workforce, Exchange, Evidence and Ledger. Control links to the complete inherited dashboard. Dedicated Skills, Tools and Memory screens are NOT YET MIGRATED, rather than empty product categories.

The UI imports a complete WorkOrder, presents it for review and uses the existing authenticated mutation API. Permission, scope, worker qualification, verification, quarantine, admission and accounting remain runtime responsibilities. Tokens stay in page memory; disconnect and failed reads clear observed state. A trusted host must register real capabilities; enabling the exchange alone does not provision workers.

Keep Agent Control separately consumable now, architecturally the underlying runtime. Do not split packages without measured need. No provider/harness dependency was introduced. DigitalWorker remains separate from Model.

Visual identity uses an original SVG junction, charcoal/graphite, silver, ivory and restrained vermilion. No robot/HUD/gradient or operational log is present in the hero. Flat vector is the production candidate; textured metal treatment is deferred. Wordmark font outlines and final small-icon brand refinement remain design review items.

## Qualification

Source: fresh npm run check, **2,334 / 2,334 PASS**, zero failures/skips. Candidate: **2,360 / 2,360 PASS**, zero failures/skips/cancellations. Difference: +26 tests (24 inherited exchange tests and 2 Tsunora tests). No functionality intentionally excluded from the suite.

The two new Tsunora regression tests failed before implementation and passed after. Focused API/auth/palette suite: 15/15 PASS. Chromium actual DOM/HTTP qualification submitted tsunora-ui-outcome through the product UI to the exchange. A real Python process completed exact arithmetic and an independent deterministic verifier accepted it. This is physical bounded deterministic work, not AI competence or arbitrary outcome planning.

The unchanged inherited deterministic qualification workload is also rerun, retaining all attempts, calibration, fixed/broker comparisons, controlled failure recovery, permissions, quarantine and containment results. Frozen workload/results and browser recordings are in the separate evidence bundle. Exact counts, hashes and full regression results are recorded there.

An intermediate complete regression found two omitted direct README links to first-run authentication and fresh Termux prerequisites (2,358 pass / 2 fail). The links were restored rather than weakening the inherited tests.

Initial negative evidence retained: dynamic-port qualification origin denied the first UI mutation; no execution occurred. The disposable harness now explicitly allowlists its actual loopback origin. The first full candidate check found a hard-coded AI qualification runtime path inherited from experimental lineage; current CLI requires explicit environment configuration for runtime/model paths. No governance check was relaxed. AI historical benchmark was not rerun after this configuration-only change.

## Classifications and limits

| Capability | Classification | Basis |
|---|---|---|
| Released Agent Control runtime | INHERITED + QUALIFIED | Prior release evidence; fresh regression. Claims retain original bounds. |
| Digital Labour Exchange / AI economics | INHERITED + EXPERIMENTAL | Small bounded workloads; economic conclusion remains neutral/inconclusive. |
| Tsunora entry, read views, auth, bounded work import | TSUNORA QUALIFIED | Narrow browser/HTTP and real deterministic transaction evidence; not a production certification. |
| Tsunora general digital-work proposition | TSUNORA EXPERIMENTAL | Arbitrary work understanding, universal qualification and production brokerage not established. |
| Dedicated Skills / Tools / Memory surfaces | NOT YET MIGRATED | Inherited controls remain accessible. |
| Public creation / publication | BLOCKED | Explicit migration review gate. |

Inherited limits remain: partial diagnostic/security coverage; original small AI-reviewed evaluation; Pixel identity remains unverified; sequential multi-resource edits; experimental research stays experimental; voice smoothness under contention unqualified; exchange single writer, local hash integrity, bounded qualification and internal accounting only. No claim of general cost saving, energy measurement, payments or production tenant isolation.

## Migration risks and next work

- Complete reachable-history privacy review before publication. Full history includes experimental machine paths; cleaning current files is not a history audit. Preserve upstream unchanged and agree a provenance-preserving public strategy before pushing.
- New AI CLI path configuration is tested structurally; historical model economics are not requalified.
- Work contract import is an engineering entry point, not a natural-language work composer.
- The new shell initially covers exchange work; general Jobs and broader capabilities use inherited controls.
- Retain deployment and runtime registration as a separate authorised operation. No protected service was modified here.

Review the exact changed-files.txt, commit state, evidence manifest and screenshots before authorising any public creation. This is a local product candidate, not a release.

## Sealed result

Fresh full regression passed at executable candidate `99c1e3fda59cc05a4442c24ffc77a4d670109aeb`. The following report-only seal changes no runtime/test source; Markdown links are rechecked after sealing. Exact final repository SHA is in FORK-STATE.json in the evidence handoff.

The physical exchange run verified 45/45 outcomes: nine distinct cases across five policies, not 45 independent tasks. There were 67 execution attempts including calibration and controls. Permission and qualification denials, quarantine exclusion, controlled worker failure recovery and STOP_CONFIRMED containment passed.

A separate browser run qualified three workers on 18 calibration executions and then submitted one real WorkOrder through Tsunora; Python returned {sum: 4} as a string value, independently verified. The verification script separately checked 588 ledger events, 86 attempts, all result hashes and accounting sums across these two runs. The readable video repeats that same case and is not additional held-out evidence.

Video: tsunora-live-review.mp4, 20.48 seconds, genuine Chromium capture attached before physical submission. Full decode and final-frame inspection PASS. SHA-256: 1e5ad0752fd941f3f9c5926b1b78ec75b7ee4fc82f3892062ba6e76564562280.

Current changed textual files passed the targeted private-endpoint/secret-pattern scan; full reachable-history privacy clearance remains outstanding before public push. Three actual workers, process identities, attempt latency and tariff-based internal charges are retained. Token use is not applicable, energy and external monetary cost are unknown; CPU resource savings are not claimed.

Local Windows checkout: work/tsunora within this task workspace. Linux qualified checkout is recorded in private FORK-STATE.json. Both push destinations are disabled. Original evidence/source archive locations and hashes are retained in inherited-artifact-hashes.txt; no historical evidence was overwritten.
