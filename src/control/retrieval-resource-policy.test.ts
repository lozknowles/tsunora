import assert from 'node:assert/strict';import test from 'node:test';import{decideRetrievalResource,type RetrievalResourceObservation}from'./retrieval-resource-policy.js';
const observation:RetrievalResourceObservation={provider:{id:'hybrid',name:'Hybrid',locality:'LOCAL',strategies:['HYBRID'],capabilities:['retrieval.search'],index:{required:true,mutationCapability:'retrieval.index.manage'}},indexState:'MISSING',availableMemoryMb:4096,availableStorageMb:4096,repositoryBytes:10_000_000,expectedTaskDurationMs:120_000,coldIndexMemoryMb:954,coldIndexDurationMs:21_410,indexManageAuthorized:true,builtinAvailable:true};
test('current warm index uses provider without mutation authority',()=>assert.equal(decideRetrievalResource({...observation,indexState:'CURRENT',indexManageAuthorized:false}).action,'USE_PROVIDER'));
test('qualified capacity and separate authority permit an index build',()=>assert.equal(decideRetrievalResource(observation).action,'BUILD_INDEX'));
test('short task or constrained node uses bounded built-in retrieval',()=>{assert.equal(decideRetrievalResource({...observation,expectedTaskDurationMs:20_000}).action,'USE_BUILTIN');assert.equal(decideRetrievalResource({...observation,availableMemoryMb:512}).action,'USE_BUILTIN');});
test('missing authority and fallback defer safely when built-in is absent',()=>assert.equal(decideRetrievalResource({...observation,indexManageAuthorized:false,builtinAvailable:false}).action,'DEFER_INDEX'));
