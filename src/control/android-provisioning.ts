import type {Resource} from './capabilities.js';import{capabilityId}from'./capabilities.js';import type{WorkExecutionResult,WorkHandler}from'./work-executor.js';import type{WorkItem}from'./work-queue.js';
export const TERMUX_BOOT_SOURCE={host:'github.com',repository:'termux/termux-boot',signingSource:'github',packageName:'com.termux.boot'} as const;
export const ALLOWED_PACKAGE_OPERATION={manager:'apt',packageName:'adb'} as const;
export type AndroidProvisioningOperation='detect-adb'|'install-adb'|'pairing-approval'|'qualify-adb'|'obtain-termux-boot'|'verify-termux-boot'|'install-termux-boot'|'verify-termux-boot-package'|'install-boot-hook'|'verify-boot-hook'|'qualify-reboot-recovery';
export interface AndroidProvisioningOperations{detectAdb():Promise<boolean>;installPackage(spec:typeof ALLOWED_PACKAGE_OPERATION):Promise<{installed:boolean}>;qualifyAdb():Promise<{qualified:boolean}>;obtainTermuxBoot(spec:typeof TERMUX_BOOT_SOURCE):Promise<{artifactRef:string;sha256:string}>;verifyTermuxBoot(spec:typeof TERMUX_BOOT_SOURCE,artifact:{artifactRef:string;sha256:string}):Promise<{verified:boolean;sha256:string}>;installTermuxBoot(packageName:string):Promise<{installed:boolean}>;verifyTermuxBootPackage(packageName:string):Promise<{installed:boolean;signingSource:'github'|'other'}>;installBootHook():Promise<{installed:boolean}>;verifyBootHook():Promise<{verified:boolean}>;qualifyRebootRecovery():Promise<{qualified:boolean}>;}
const id=(name:string)=>`android.provision.${name}`;
const base=(name:string,operation:AndroidProvisioningOperation,requires:string[],dependsOn:string[]=[],status:WorkItem['status']='queued'):WorkItem=>({id:id(name),type:`android.${operation}`,class:'priority',status,capabilities:{requires:requires.map(id=>({id}))},createdAt:new Date().toISOString(),batchable:false,preemptible:false,dependsOn,attempts:0,maxAttempts:2,data:{operation,executionClass:'control',controlOperation:'android-provisioning'}});
export function androidProvisioningWorkItems():WorkItem[]{const detect=base('detect-adb','detect-adb',[capabilityId.shell]);const install=base('install-adb','install-adb',[capabilityId.shell,capabilityId.packageInstall],[detect.id]);install.data={...install.data,operation:'install-adb',manager:'apt',packageName:'adb'};const pairing=base('pairing-approval','pairing-approval',[capabilityId.codex],[install.id]);pairing.data={...pairing.data,operation:'pairing-approval',reviewGate:true};const qualify=base('qualify-adb','qualify-adb',[capabilityId.adb],[pairing.id]);const obtain=base('obtain-termux-boot','obtain-termux-boot',[capabilityId.networkRead],[qualify.id]);const verify=base('verify-termux-boot','verify-termux-boot',[capabilityId.shell],[obtain.id]);const installApk=base('install-termux-boot','install-termux-boot',[capabilityId.adb,capabilityId.androidPackageInstall],[verify.id]);const verifyApk=base('verify-termux-boot-package','verify-termux-boot-package',[capabilityId.adb],[installApk.id]);const hook=base('install-boot-hook','install-boot-hook',[capabilityId.ssh,capabilityId.repoWrite,capabilityId.codex],[verifyApk.id]);const verifyHook=base('verify-boot-hook','verify-boot-hook',[capabilityId.ssh],[hook.id]);const reboot=base('qualify-reboot-recovery','qualify-reboot-recovery',[capabilityId.adb,capabilityId.ssh],[verifyHook.id]);return[detect,install,pairing,qualify,obtain,verify,installApk,verifyApk,hook,verifyHook,reboot];}
export function createAndroidProvisioningHandler(ops:AndroidProvisioningOperations):WorkHandler{return async(work,_resource,context)=>{const operation=work.data?.operation as AndroidProvisioningOperation;switch(operation){case'detect-adb':return{resultRef:await ops.detectAdb()?'adb-present':'adb-missing'};case'install-adb':{if(work.data?.manager!=='apt'||work.data?.packageName!=='adb')return{error:'package_operation_not_allowlisted',retryable:false};if(context.dependsOn.some(d=>d.id===id('detect-adb')&&d.resultRef==='adb-present'))return{resultRef:'adb-already-present'};try{const r=await ops.installPackage(ALLOWED_PACKAGE_OPERATION);return r.installed?{resultRef:'apt:adb-installed'}:{error:'adb_package_not_installed',retryable:false};}catch(error){return{error:error instanceof Error?error.message:String(error),retryable:false};}}case'pairing-approval':return{resultRef:'android-wireless-debugging-pairing-approved',confidence:0};case'qualify-adb':return qualified(await ops.qualifyAdb(),'transport.adb');case'obtain-termux-boot':{const r=await ops.obtainTermuxBoot(TERMUX_BOOT_SOURCE);return{resultRef:JSON.stringify(r),fingerprint:r.sha256};}case'verify-termux-boot':{const r=await ops.obtainTermuxBoot(TERMUX_BOOT_SOURCE),v=await ops.verifyTermuxBoot(TERMUX_BOOT_SOURCE,r);return v.verified&&v.sha256===r.sha256?{resultRef:'github-termux-boot-verified',fingerprint:v.sha256}:{error:'termux_boot_provenance_or_hash_failed',retryable:false};}case'install-termux-boot':{const r=await ops.installTermuxBoot(TERMUX_BOOT_SOURCE.packageName);return r.installed?{resultRef:'adb:termux-boot-installed'}:{error:'termux_boot_install_failed',retryable:false};}case'verify-termux-boot-package':{const r=await ops.verifyTermuxBootPackage(TERMUX_BOOT_SOURCE.packageName);return r.installed&&r.signingSource==='github'?{resultRef:'github-termux-boot-package-verified'}:{error:'termux_boot_signing_source_mismatch',retryable:false};}case'install-boot-hook':{const r=await ops.installBootHook();return r.installed?{resultRef:'termux:boot-agent-control-hook-installed'}:{error:'boot_hook_install_failed',retryable:false};}case'verify-boot-hook':{const r=await ops.verifyBootHook();return r.verified?{resultRef:'termux:boot-agent-control-hook-verified'}:{error:'boot_hook_verification_failed',retryable:false};}case'qualify-reboot-recovery':return qualified(await ops.qualifyRebootRecovery(),'android.unattended.recovery');default:return{error:'unknown_android_provisioning_operation',retryable:false};}};}
function qualified(r:{qualified:boolean},cap:string):WorkExecutionResult{return r.qualified?{resultRef:`qualified:${cap}`}:{error:`qualification_failed:${cap}`,retryable:false};}
export function isAndroidProvisioningResource(resource:Resource){return resource.capabilities.some(c=>c.id===capabilityId.codex);}
