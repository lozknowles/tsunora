# Agent Control 4.8.1 release verification

Agent Control 4.8.1 is published only after the exact public-main source passes source reconciliation, neutrality and security review, the complete automated suite, source-distribution checks, a clean archive installation, backward-compatibility checks, retained-evidence projection, artifact inspection and checksum verification.

## Qualified change

The release is a direct additive descendant of public v4.8.0. It contains the governed native benchmark lifecycle, same-run resume and attempt-bound cleanup/recovery work, plus evidence-driven speculative-decoding qualification and routing. Navigable Workspaces remains a projection over the existing authoritative Estate, run, invocation and evidence records.

The source and public tag SHA, test counts, archive digest and independent-download verification are recorded in the machine-readable release gate and release manifest attached to the GitHub v4.8.1 release. Those immutable assets are the publication receipt; this document does not predict commit identifiers generated by the governed integration step.

## Retained physical evidence

The speculative-decoding release gate uses the already-completed, content-addressed qualification artifact. Release sealing does not rerun the physical benchmark.

- Run: `run-c6446301-3c4e-4e47-8d3f-e5f0090dc0d2`
- Artifact: `artifact-b74a3b06-d3e1-4e1e-b7eb-b13a1ec8abcd`
- SHA-256: `40a4f272380be5c05fbb2ec2e2704219e2c37617a09dadb0909e217d32616224`
- Result: `BENEFICIAL`
- Main model: Qwen3-8B
- Draft model: Qwen3-0.6B
- Best candidate: two draft tokens
- Median throughput improvement: 23.80%
- Median TTFT change: +24.48%
- Acceptance: 82.98%

The release source must parse and project this artifact with matching identity, digest, metrics and deterministic routing decisions. The artifact itself remains outside the public source distribution.

## Mandatory release gates

The attached release-gate JSON records binary results for source reconciliation, complete tests, neutrality, security, clean archive installation, backward compatibility, retained-evidence integrity, protected-service non-interference, archive inspection, checksums, public main, tag, release assets and documentation. Any failed, blocked, unknown or unverified mandatory gate prevents the tag and GitHub release. Publication does not authorize deployment to operational installations.
