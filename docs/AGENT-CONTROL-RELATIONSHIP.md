# Powered by Agent Control

Agent Control remains an independent project. Tsunora retains its runtime and history and introduces a product identity and work-first workspace. It is neither an upstream rename nor a new origin for historical evidence.

Keep Agent Control separately consumable. Treat it as the foundational runtime in the product architecture, without prematurely splitting source packages. The existing agent-control package, bin, API and schema identifiers are compatibility contracts. Product identity is separately recorded in tsunora-product.json.

No upstream remote, main branch, tag or serving installation is changed by this fork. Public creation/push requires the repository migration review in the user brief.
