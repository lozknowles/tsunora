# Agent Control 4.6 containment status

Two older dashboard instances were found behind overlay-network HTTPS proxy publications. Those publications were removed because the installed versions predate authenticated control-plane reads. Both controller processes remain loopback-bound and running. SSH access, unrelated overlay configuration and unrelated services were preserved.

Raw before/after overlay configuration, listener inventory and hashes are retained outside the repository in the owner-only stabilisation evidence directory. No credentials are present in the containment or verification outputs. Re-exposure is blocked until an authenticated remediated build is installed and qualified.
