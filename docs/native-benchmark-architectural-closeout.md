# Atlas-native benchmark architectural closeout

## Independent verdicts

- Public Agent Control v4.8.0: UNCHANGED.
- Atlas benchmark execution: PASS.
- Codex required for execution: NO.
- Phi native physical executions: 27/27 SUCCEEDED.
- Phi quality: FAILED / BELOW QUALIFICATION THRESHOLD, 15/27 passed, 12 failed, 0 unknown.
- Reproducibility: all 27 outputs byte-identical and all 27 validator outcomes identical, with unchanged per-fixture hashes.
- Service restoration: PASS, 27 confirmations and retained healthy/idle post-run observation.
- Benchmark capability: PASS for the documented existing-artifact llama.cpp adapter boundary.
- Pixel model study: INCOMPLETE.
- Smol: READY_TO_RESUME study progression, subject to its ordinary fresh safety/authority/model checks. Not dispatched or pre-admitted by this closeout.
- Agent Control Lab: READY_TO_PACKAGE; isolated review package prepared.
- Release recommendation: RECOMMEND v4.8.1; no publication authorised here.

## Source and evidence

Requested candidate f773a3daba62874f27e1ca2bb288e292c9699ac5 is an ancestor of the actual physical run source 86781560cd1d6899c5a81d949fadd3f649e2e942. Retained source 9d5766e187e48634741cc349f16da6c42ab066c9 differs from physical source only in documentation. This sealing task adds documentation and Lab projections, not runtime changes.

Run `run-75f56123-e05e-4d08-8034-67d5a602c0db`, 2026-09-16 04:07:51.493Z to 04:39:51.387Z. The closeout re-read and SHA-256 verified all 4,291 native artifacts and 374 original Phi/Smol artifacts. No historical artifact or assessment was overwritten. Private CONTINUOUS_EXECUTION_TRACE.json contains per-invocation ownership, owned process completion, helper identity, lifecycle timestamps, scorer decisions and artifact/checksum references. Public per-fixture comparison omits private infrastructure.

## Continuous ownership chain

Authenticated Agent Control request -> sealed model-hardware-qualification job -> runtime-benchmark worker -> Atlas product execution capability -> TargetLlamaRuntime -> owned SSH/ADB transport -> physical Pixel Android/Termux -> target observation -> admission -> verified runtime/model -> retained response/tokens -> product validator -> ArtifactStore -> restoration/postflight.

The runtime records bind producer, worker, adapter, target, environment, transport, run and step. Battery, charging, thermal and RAM probes are product target observations; admission is recorded by the product policy. Runtime lifecycle records show service suspension, post-reclamation memory admission, start, inference completion, restoration and retained result. Each of 27 receipts has a completed owned process and no missing provenance requirements. Python scoring has the unchanged sandbox-validator hash. No qualification-side operational executor is needed in this chain.

Atlas is the production execution capability described here, not a claim of a separate new service. The worker runs on the controller; inference runs on the Pixel. Trusted-controller provenance is not cryptographic attestation against a malicious administrator.

## Gate correction

The previous report's BLOCKED framework conclusion conflated the model gate with the framework gate. That report remains immutable. This assessment supersedes that conclusion, not its measurements.

Source `model-hardware-qualification.ts` records per-attempt execution status, quality and evidence completeness independently; its final qualification aggregate requires every model quality result to pass. That is a valid model gate. It contains no rule that the next study model may run only if Phi qualifies. All 27 matching outputs and recorded per-fixture validator outcomes support a genuine negative model result, not a scoring defect.

Minimum correction: label framework, model, safety and progression verdicts separately in reports/operator guidance. No scorer or runtime-policy change is justified. Framework PASS permits consideration of Smol; fresh safety checks still govern actual execution. Smol's eight external-harness calls remain historical, not native continuation credit.

## Reusable capability assessment

Owned jobs, target/runtime adapters, telemetry, charging/battery/RAM/thermal admission, refusal-before-execution, lifecycle, restoration, frozen fixtures, product scoring, native receipt validation, false-native rejection, evidence preservation, CLI, configuration and documentation are present. Retained physical records prove the normal path and earlier refusal; automated negative cases cover failure/cancellation/restoration and forged claims. See the responsibility matrix and scoped harness escape inventory.

The standalone CLI definition and status commands succeeded against the retained real controller. Seven focused CLI/provenance tests freshly passed, including exact job submission and external-process native-claim rejection. Together with the retained receiving job chain this proves the interfaces do not require Codex. No new model dispatch was used for this check; no claim of a fresh CLI-origin physical run is made.

Full suite: retained 1,665 passed, 0 failed, 0 skipped, 174422.729734 ms. Production files are byte-identical to tested source; only documentation/data packaging changed during closeout. Full suite was not rerun merely for editorial changes. Tests establish covered cases, not universal runtime/platform correctness.

## Ownership and packaging

Agent Control is the authoritative runtime: jobs, Atlas execution, adapters, admission, telemetry, scoring, lifecycle, provenance and evidence. Lab holds definitions, model/fixture manifests, sanitized datasets, charts and reproduction guides. Existing Lab README/package structure supports this boundary. The new package contains no SSH executor, copied runtime engine, recovery script, private address, device serial or credential reference.

## Limits retained

Existing llama.cpp artifacts only; acquisition unsupported. One configured target/model/suite per registration. Python scoring requires the Linux sandbox. Other runtimes/platforms require their own qualification. No GPU/power/throttling values are manufactured. Retained Markdown includes all 27 inference sections but the later bulk operations appendix reaches its existing 4 MiB cap; structured artifacts remain complete. Historical/native performance conditions differ and do not prove acceleration.

No Phi rerun, Smol execution, model download, physical-device mutation, public push/tag/release, deployment or change to v4.8.0 occurred in this closeout.
