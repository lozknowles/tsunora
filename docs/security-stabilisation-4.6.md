# Agent Control 4.6 security stabilisation

**Release status: 4.6.1 candidate.** This remediation is based on reviewed snapshot `ca15a4b7ebd8a87bce667a132049121f96db5898`. Publication requires the exact-candidate schema-v2 gate and external protected approval described below.

The completed source fixes bind release evidence to exact source and protected approval, confine repository snapshots, authenticate control-plane reads, enforce browser destination policy, preserve late-output redaction, bound session leases, require current model qualification, surface capability-action failures, isolate governed Git subprocesses, remove stored session-share credentials, stabilise evidence-led energy routing, recover orphaned intelligence locks, retain prior qualification output, classify future benchmark failures, and bound disruptive maintenance.

The original review packets and failing benchmark artifact remain unchanged. The complete disposition register is [agent-control-4.6-security-stabilisation-disposition.json](provenance/EXTERNAL-EVIDENCE.md).

## Verification contract

- The exact 4.6.1 candidate must pass the complete repository check, focused security regressions, clean package inspection, virgin installation and 4.6.0 upgrade.
- The hardened release gate requires a schema-v2 receipt and individual content-addressed evidence for every core check.
- The receipt binds the exact source commit, source digest, version and archive digest. Its protected operator approval remains outside the candidate tree.
- Operational containment and verification outputs are scanned for credential patterns. Deliberate synthetic redaction fixtures remain clearly test-scoped.

## Capability boundaries

- Shared-token ACP transports are supported as one-principal scopes. No distinct-principal isolation is claimed without independently authenticated principals.
- Discovery command timeouts use the shared owned-process-tree adapter so Windows and Linux descendants follow the same bounded cleanup path.
- Model benchmark promotion remains blocked until approved target execution and independent acceptance evidence exist. No winner is recorded.
- Whole-node energy and paid-provider billing remain externally blocked where no authorised meter binding or metered provider is configured. Component telemetry stays component-scoped.

These honest capability limits do not block the security patch because 4.6.1 does not advertise the unavailable measurements, model result or multi-principal transport.

## Release and deployment boundary

Publication requires a clean exact candidate, complete gate PASS, normal public-history integration, annotated tag and artifact digest verification. Repository publication does not deploy 4.6.1 over existing estate services; operational installation remains a separate authorised action.
