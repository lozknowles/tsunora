# v4.14 RC2 limitations

- Security coverage remains PARTIAL. The v4.13 diagnostic evaluation was only 47 correlated AI-reviewed cases. Do not combine it with the prior 264-event research reconciliation or later mechanical lifecycle checks.
- Pixel physical identity remains unverified. CPU-only Ubuntu and bounded Linux-host evidence do not establish universal platform support.
- SEMANTIC_TOOL_V1 and its installed qualification adapter remain explicitly gated and disabled by default. Five internal tasks do not establish independent generalisation or universal model capability.
- Deterministic repair does not invent arguments, normalise types/enums or expand authority. Strict native semantic calls do not fall back to legacy JSON repair. Zero false repairs on a frozen corpus is not universal safety.
- Numbered protection covers MutationWorkspace writes and exact replacements. Source/read provenance is bounded to retained workspace observations. Syntax differential checks cover JavaScript fixture files; arbitrary patch APIs and all programming languages are not qualified. Text bytes alone cannot prove the origin of an ambiguous pipe-numbered example.
- Multi-file changes remain sequential, not atomic. An earlier valid write can remain after a later write fails. Atomic operations are DEFERRED.
- No-progress exceptions depend on trustworthy classification. Scripted recovery demonstrates controller transitions, not autonomous model planning. Slow generation without completed tools is distinct from a tool loop.
- Provider usage and canonical accounting attestation are distinct. Unavailable token/cache fields stay unknown; controlled HTTP fixture numbers are not model usage. Energy is not claimed without measurement.
- The synthetic forensic vault remains EXPERIMENTAL_DISABLED for production. General private-data permissions, revocation, retention enforcement and all-secret redaction are not qualified. Public packages must exclude runtime and forensic state.
- New evidence may be forward-only. The final downgrade receipt must distinguish preservation from native v4.13 semantic interpretation and prove old operational state remains usable.
- Twelve identity colour slots can collide; explicit Job/Run IDs and status labels remain necessary.
- AutomationBench and Strands are EXCLUDED. AutomationBench is not qualified; AWS Strands cost claims remain unreproduced.
- Videos are unsigned observations. Controlled HTTP faults and actual model inference must be labelled separately. No replay may be presented as live execution.
- RC1 outcomes and failed pilot attempts are retained as history, not passed off as exact-final-commit RC2 qualification. Consult the final sealed report for every remaining blocker.
