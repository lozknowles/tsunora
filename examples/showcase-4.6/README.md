# Captured 4.6 candidate example

This is a real initial metadata observation and readiness proposal, captured on 13 September 2026. It is not a measured overnight benchmark.

| Stage | Recorded outcome |
|---|---|
| Initial model/artifact/runtime changes | 17 |
| Relevant and provisionable reviewed candidates | 2 |
| Models benchmarked | 0 |
| New leader | None established |
| Execution status | APPROVAL_REQUIRED |

[Machine-readable observation](model-intelligence.json) retains the native parcel identity and evidence hashes. The full private evidence store contains the source snapshots and shortlist; private workload payloads and machine paths are excluded here.

The other artifacts lacked reviewed context/resource evidence. The two pinned Q4_K_M artifacts had exact reviewed identity, size and hash matches. Resource planning inputs remain estimates; target-model capability has not been qualified.

## What changed while I slept?

**No new leader established.** Two candidates can be proposed for your Python repair workload. The benchmark requires approval before model acquisition. No routing change is recommended from metadata alone.

This heading is the product experience. The actual capture was a short read-only run, not a claim that a watch ran overnight.

## Benchmark evidence

No performance league table or canonical measured result is published yet. Download approval and target execution are outstanding. A release claiming a completed benchmark showcase must fail the SHOWCASE gate until genuine comparable results, validator evidence and resource measurements are provided.

## Release integration follow-up

The [new source runs](release-integration/real-intelligence.json) preserve a separate initial baseline (17 observations, two reviewed candidates) and follow-up (zero observed changes). Both disclose 2/3 complete source coverage, no target execution and no new leader. The [current RC assessment](../../docs/release-candidate-4.6.md) separates core readiness from the complete target-model showcase.
